# MRVL 标准 T1：用户明确批准（2026-08-31）

## 现行规则

用户在本任务对“是否确认 MRVL 采用 T1，低／中／高情景系数
60%／80%／100%”明确回复“确认”。本次将该批准记录于
2026-08-31 22:05 HKT；记录时点不是重新取数时点，也不冒充消息的精确发送秒数。
批准编号为 `WU-20260831-MRVL-T1`。

自本次规则生效起，Webull 已核实身份的 MRVL 采用标准 T1，计入 AI
压力分子。此前暂不计入的已采纳意见和原始回执保留为历史，不再作为
现行排除依据。无需用户再次确认相同分类，也不新增待决定事项。

机器规则为 `claude/xuan-ib-ai-tier-overrides-v1.json`。普通资产分类、
投资风格分类和 AI tier 仍相互独立；不能据此给其他证券推定 tier。
普通新报告使用 `scripts/xuan-ib-mrvl-t1.mjs` 的计算器，核实证券、组合、
持仓身份及本期美元市值后分别乘 0.60／0.80／1.00，未舍入值进入分子，
最后统一舍入。身份或市值缺失不能默认为零。

这是内部压力情景计量，不是跌幅预测、AI 收入比例或交易建议。
不修改 TSEM／IREN 特例、其他证券系数、ETF 穿透权重、三账户含现金分母、
20%／25%／30% 线、GOOG 规则、四桶或现金规划。不授权任何账户写入、交易或转账。

## 当前同一快照的受控更新

本次已使用实际 Claude CLI 的 tools-disabled、plan 模式独立复算。
Claude 同意计算，并提出三项限定，均纳入本契约：阈值距离明确只指
中情景；规则维护与单文件报告候选分开；新窄例外单独记录，不能套用
现金规划例外。MRVL 只按已核实 Webull 身份纳入，不对其他账户臆加
同名持仓；原 Webull 分子仅含 GOOG 与 IREN，须核对无重复计入。

为立即体现用户刚批准的规则，允许一次且仅一次 source-blob 绑定的风险重算：

- 源 `fdf1aa6ebb87e56fc5c1a640838e8397bfc68b20`；HTML blob
  `ef5c5be7d078eafd8d4a82f72d0435fa6e7c17eb`。
- 保留原 PM 日期和 2026-08-31 20:55–20:58 HKT 数据时间，MRVL 市值
  21,662、三账户分母 6,141,014 及所有其他原金融输入不变。
- 按原表显示输入重新计算中情景，修正原表的约一美元舍入差；每行独立
  舍入，总额以未舍入值汇总，二者可能不恰好相加。不能宣称恢复了源数据
  中未在原报告给出的精度。
- 低、高原组合基数仍为历史比例近似，只加 MRVL 的准确分类增量。
  必须标“约”并说明不是全组合逐票三档计算，不能以近似高情景作精确
  30% 警戒线判断。正常新报告有完整三档来源时再逐票复算。
- 同步摘要、AI 表、阈值距离、MRVL 当前说明和报告说明；原三条回执、
  decision-state、其他金融表、现金规划、分类披露和日期／版别均保留。
- 显著说明：同一快照的批准口径更新，未重新取数，不是新一轮 AM／PM
  运行成功证据。不得追溯改写历史档案，不得覆盖比此源更新的报告。

此窄例外优先于常规报告的 fresh-read 要求，仅适用于上面的确定输入，
不是 `records-update`，不扩展日期窗口、签名或发布权限。由 Claude 从
当前 main 创建单文件 `xuan-ib/index.html` 候选，运行可信 guard，然后
Validate → Promote → Pages；主代理不得直接修改 latest/meta 或跳过保护。
普通 AM／PM／临时报告仍必须执行原定只读取数和验证。

落实证据通过独立维护 PR 追加到进度账本。只有线上新配对已回读、金额和
原回执验证通过，才将本次风险重算记为已核验；规则已保存本身不是发布成功。
