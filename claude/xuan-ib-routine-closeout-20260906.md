# Routine classification closure and concise mobile notes

Owner confirmed 2026-09-06: routine risk-tier classification under the existing
coefficients is delegated. Codex is accountable for implementation, calculation,
publication and follow-up; Claude provides independent review when available.
Do not label tasks jointly owned with no single accountable party.

## Completion and notification

- Verify identity, dated value and source/business evidence; assign an existing
  supported tier, calculate against the matching account scope, then publish and
  read back. Only then issue one owner notification and archive that rule revision.
- `calculateAaoiT1` emits the stable per-rule `notifyId`. It does not send a
  message. Record actual notification delivery against that ID in the release
  audit; a generated ID or a prepared candidate is not delivery or completion.
- AAOI is explicitly approved T1, low/mid/high 60/80/100%. Do not reopen its
  approval or exclude it merely because the next report has a new fingerprint.
  For current AAOI holdings, use their applicable dated USD values, not the
  historical correction's amount. Missing values remain a Codex-owned data
  exception, not zero and not an owner classification decision.
- MRVL and GOOG's established rules continue without new approvals. Every
  report's source freshness is still checked separately from rule adoption.
- Evidence-insufficient classifications are not guessed. Put the exact issue,
  Codex owner and next action in the technical record. Material uncertainty
  affecting risk assessment remains briefly visible. Ask the owner only for a
  missing decision, unavailable login or operation genuinely requiring them.
- No delegated trade, cancel, transfer, account write, coefficient/threshold
  change or account-scope expansion. Original receipts remain immutable.

## This historical correction

`xuan-ib-aaoi-snapshot.mjs` permits exactly one deterministic transformation of
source3bc2f72242fdee745dac8241064e82d79186e14b / blob078ab055e58d3e892370954adc547929dfba68f6.
It adds AAOI to the original 2026-09-05 snapshot and re-sums the displayed risk
rows, keeping the dated denominator unchanged. Low/high are still approximate.
The expected new middle scenario is21.89%, not a fresh market observation.

The guard compares the ENTIRE candidate to that deterministic result before
treating account authorization as historical. No marker-only permission, new
live read or session bypass exists; any other changed byte or source pair fails.
An independent normalization check preserves every byte outside four explicitly
allowed risk fragments, and the expected output blob is pinned as well. The
source-bound correction becomes ineligible once promotion advances the trusted
latest pair; no repeated add-on can be applied. The maintenance implementation
must be approved and merged before a separate one-commit candidate can use it.
The corrected candidate uses the normal one-commit source branch, trusted guard,
promotion, metadata pairing and Pages publication. It is not a new adhoc trial
or evidence that an AM/PM run succeeded. Re-running on a corrected or newer
source fails; normal subsequent reports must not copy this one-time marker.

## Mobile reading

- Main To do retains original unanswered decisions and unresolved explicit owner
  actions from the latest valid receipt-bound events. A report-pair change does
  not resolve an owner request; label an older request as historical, without
  claiming current numerical verification. Technical progress is not an owner
  tasks. The bulky historical implementation fold is hidden, not falsely closed
  and not deleted from the append-only source ledger.
- Remove the unused `已结案 / 只读观察` entry. Do not revive canceled four-bucket
  features or historical temporary-report controls.
- Each Report notes starts with 2-4 short useful points. Preserve source dates,
  same-scope denominators, approximation/missing-data limits and cash-planning
  versus buying-power distinctions. Current material data-conflict exceptions
  remain visible in full; irrelevant historic logs and duplicated prose do not.

## Independent design review

Claude Fable5.1 / Max completed a technical-prose review on2026-09-06, without
access to code or financial data. Incorporated independent outside-fragment
confinement, pinned result identity, persistent unresolved owner-request display,
and counted expandable data-limit details. Existing receipt-bound append-only
events remain authoritative. This is design review, not independent code signoff.

Do not expand this into a new notification service: no such service is in scope.
The release owner sends one completion message and records delivery in the
release audit. Market-value updates under the same established tier do not
reopen classification approval. New classifications, changed rule/evidence basis,
or material exceptions are distinct events, not suppressed by the prior notice.
